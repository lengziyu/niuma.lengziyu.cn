fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
python-multipart>=0.0.9,<1
pdf2docx>=0.5.8,<1
PyMuPDF>=1.24,<2
python-docx>=1.1,<2

# System dependency (not pip-installable):
# LibreOffice is required for high-quality PDF to Word conversion.
# Install via: sudo apt install libreoffice-writer (Ubuntu/Debian)
#          or: sudo yum install libreoffice-writer (CentOS/RHEL)
# If LibreOffice is not found, the service falls back to pdf2docx (lower quality).
